# Security

Do not report the built-in L1 guarded runtime as an operating-system sandbox. Review `docs/SAFETY.md` before production use.

For security issues in the package itself, disclose privately to the package maintainers rather than opening a public issue containing exploit details.
